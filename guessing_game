import random
target = random.randint(0, 100)

while True: # 進入無限迴圈
  num = input('請輸入數字:') # input輸入的會是字串型態
  print('你輸入的數字是:', num)
  if int(num) < target: # 所以這邊必須強制轉型
    print('再猜大一點')
    
  elif int(num) > target:
    print('再猜小一點')
  else:
    print('猜中了')
    break
  
  
